// <-- CLI SHELL MODE -->
scicv_Init();
Attention : Redéfinition de la fonction : BriefDescriptorExtractor. Utilisez funcprot(0) pour ne pas afficher ce message.

img = imread('Data/images/Puffin.png',CV_LOAD_IMAGE_GRAYSCALE);
//morphological opening (remove small objects from the foreground)
out = dilate(img,getStructuringElement(MORPH_ELLIPSE,[10 10]));
rows_img = Mat_rows_get(img);
rows_out = Mat_rows_get(out);
assert_checkequal(rows_img, rows_out);
